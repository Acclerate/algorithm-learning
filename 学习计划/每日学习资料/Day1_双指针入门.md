# 📚 Day 1 学习资料：双指针入门

> **日期**: 2026-02-05
> **主题**: 数组与双指针
> **目标**: 理解双指针如何避免嵌套循环，优化时间复杂度

---

## 🎯 今日学习目标

- [ ] 理解双指针的核心思想
- [ ] 掌握"从后往前"的双指针（LeetCode 88）
- [ ] 掌握"快慢指针"（LeetCode 27）
- [ ] 能独立写出两种双指针的代码

---

## 📖 题目 1：LeetCode 88 - 合并两个有序数组

### 问题描述
```
给你两个按 非递减顺序 排列的整数数组 nums1 和 nums2，
另有两个整数 m 和 n ，分别表示 nums1 和 nums2 中的元素数目。

请你 合并 nums2 到 nums1 中，使 nums1 成为一个有序数组。

合并后，数组不应由函数返回，而是存储在数组 nums1 中。
为了应对这种情况，nums1 的初始长度为 m + n，
其中前 m 个元素表示应合并的元素，后 n 个元素为 0 ，应忽略。
nums2 的长度为 n。
```

### 示例
```
输入：nums1 = [1,2,3,0,0,0], m = 3
      nums2 = [2,5,6],       n = 3
输出：[1,2,2,3,5,6]

输入：nums1 = [1], m = 1
      nums2 = [],  n = 0
输出：[1]

输入：nums1 = [0], m = 0
      nums2 = [1], n = 1
输出：[1]
```

### 💡 核心思想：从后往前双指针

#### 为什么不能从前往后？

```
从前往后的问题：
nums1 = [1,2,3,0,0,0], nums2 = [2,5,6]

如果从前往后合并：
步骤1: 比较 1 和 2，1 小，放 1 → [1,2,3,0,0,0]
步骤2: 比较 2 和 2，相等，放 nums2 的 2 → [1,2,3,0,0,0]
                                     ❌ 问题：覆盖了 nums1 的 2！

❌ 结果：nums1 的原始数据被覆盖，丢失了！
```

#### 从后往前的优势

```
从后往前合并：
nums1 = [1,2,3,0,0,0], nums2 = [2,5,6]

指针位置：
        p1
nums1 = [1,2,3,0,0,0]
              p
nums2 = [2,5,6]
        p2

步骤1: 比较 3 和 6，6 大，放 6
nums1 = [1,2,3,0,0,6]
              p      p-1

步骤2: 比较 3 和 5，5 大，放 5
nums1 = [1,2,3,0,5,6]
          p    p-1

步骤3: 比较 3 和 2，3 大，放 3
nums1 = [1,2,3,3,5,6]
        p-1

步骤4: 比较 2 和 2，相等，放 nums2 的 2
nums1 = [1,2,2,3,5,6]
    p-1

步骤5: 比较 1 和 null，放 1
nums1 = [1,2,2,3,5,6]
  p-1

✅ 完成！不会覆盖原有数据
```

### 💻 代码实现

```java
class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        // 1. 设置三个指针
        int p1 = m - 1;          // nums1 有效部分的最后一个元素
        int p2 = n - 1;          // nums2 的最后一个元素
        int p = m + n - 1;       // nums1 数组的最后一个位置
        
        // 2. 从后往前比较和填充
        while (p1 >= 0 && p2 >= 0) {
            if (nums1[p1] > nums2[p2]) {
                nums1[p] = nums1[p1];  // 放 nums1 的元素
                p1--;
            } else {
                nums1[p] = nums2[p2];  // 放 nums2 的元素
                p2--;
            }
            p--;
        }
        
        // 3. 如果 nums2 还有剩余元素，直接复制
        while (p2 >= 0) {
            nums1[p] = nums2[p2];
            p2--;
            p--;
        }
        
        // 注意：如果 nums1 还有剩余，不需要处理，
        // 因为它们已经在正确的位置了
    }
}
```

### 🎨 图解过程

```
初始状态:
nums1: [1,2,3,0,0,0]
         p1       p
nums2: [2,5,6]
         p2

循环1: nums1[p1]=3 <= nums2[p2]=6
nums1: [1,2,3,0,0,6]
       p1       p-1
nums2: [2,5,6]
       p2    p2-1

循环2: nums1[p1]=3 <= nums2[p2]=5
nums1: [1,2,3,0,5,6]
       p1    p-1
nums2: [2,5,6]
       p2 p2-1

循环3: nums1[p1]=3 > nums2[p2]=2
nums1: [1,2,3,3,5,6]
    p1 p-1
nums2: [2,5,6]
   p2-1

循环4: nums1[p1]=2 <= nums2[p2]=2
nums1: [1,2,2,3,5,6]
 p1 p-1
nums2: [2] ← p2 变成 -1，退出主循环

剩余处理: p2 < 0，无需处理
最终: [1,2,2,3,5,6]
```

### ⚠️ 边界情况

```java
// 边界 1: nums1 全为 0
nums1 = [0], m = 0, nums2 = [1], n = 1
输出: [1]
✅ p1 = -1，跳过主循环，直接复制 nums2

// 边界 2: nums2 为空
nums1 = [1], m = 1, nums2 = [], n = 0
输出: [1]
✅ p2 = -1，跳过所有循环，nums1 保持不变

// 边界 3: 正常情况
nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
输出: [1,2,2,3,5,6]
✅ 标准情况
```

### 📊 复杂度分析

- **时间复杂度**: O(m + n)
  - 每个元素只访问一次
- **空间复杂度**: O(1)
  - 只用了 3 个指针变量

### 🔑 核心模式总结

**模式名称**: 从后往前的双指针

**适用场景**:
- 两个有序数组合并
- 从后往前修改，避免覆盖

**关键技巧**:
1. 三个指针：p1, p2, p
2. 从末尾开始比较
3. 每次选择较大的元素放到末尾
4. 注意处理剩余元素

---

## 📖 题目 2：LeetCode 27 - 移除元素

### 问题描述
```
给你一个数组 nums 和一个值 val，
你需要 原地 移除所有数值等于 val 的元素。
元素的顺序可能发生改变。
然后返回 nums 中与 val 不同的元素的数量。

假设 nums 中不等于 val 的元素数量为 k，
要通过测试用例，你需要执行以下操作：
- 更改 nums 数组，使 nums 的前 k 个元素包含不等于 val 的元素。
- nums 的其余元素和 nums 的大小不重要。
- 返回 k。
```

### 示例
```
输入：nums = [3,2,2,3], val = 3
输出：2, nums = [2,2,_,_]
解释：你的函数函数应该返回 k = 2，
     并且 nums 中的前两个元素均为 2。

输入：nums = [0,1,2,2,3,0,4,2], val = 2
输出：5, nums = [0,1,3,0,4,_,_,_]
解释：你的函数应该返回 k = 5，
     并且 nums 中的前五个元素为 0, 0, 1, 3, 4。
```

### 💡 核心思想：快慢指针

#### 什么是快慢指针？

```
快指针 (fast): 探索者，快速前进
慢指针 (slow): 标记者，慢速前进

核心思想:
- 快指针遍历整个数组
- 慢指针指向"有效元素"的末尾
- 当快指针找到有效元素时，将其复制到慢指针位置，然后慢指针前移
```

#### 图解过程

```
nums = [3,2,2,3], val = 3

初始状态:
       slow
nums = [3,2,2,3]
       fast

步骤1: nums[fast]=3 == val=3
       → fast 跳过
       fast
nums = [3,2,2,3]
       slow

步骤2: nums[fast]=2 != val=3
       → 复制到 slow 位置
       → slow 前移
       → fast 前移
       slow
nums = [2,2,2,3]
            fast

步骤3: nums[fast]=2 != val=3
       → 复制到 slow 位置
       → slow 前移
       → fast 前移
       slow
nums = [2,2,2,3]
               fast

步骤4: nums[fast]=3 == val=3
       → fast 跳过
       fast 超出边界，退出

结果: slow = 2
前 2 个元素为 [2,2]
```

### 💻 代码实现

```java
class Solution {
    public int removeElement(int[] nums, int val) {
        // 1. 慢指针：指向有效元素的末尾
        int slow = 0;
        
        // 2. 快指针：遍历整个数组
        for (int fast = 0; fast < nums.length; fast++) {
            // 3. 如果当前元素不等于 val
            if (nums[fast] != val) {
                // 复制到 slow 位置
                nums[slow] = nums[fast];
                // slow 前移
                slow++;
            }
            // 如果等于 val，fast 继续前进，slow 不动
        }
        
        // 4. 返回有效元素的数量
        return slow;
    }
}
```

### 🎨 详细图解

```
nums = [0,1,2,2,3,0,4,2], val = 2

初始:
  s f
[0,1,2,2,3,0,4,2]

i=0: nums[0]=0 != 2
  [0,1,2,2,3,0,4,2]
   s f

i=1: nums[1]=1 != 2
  [0,1,2,2,3,0,4,2]
     s f

i=2: nums[2]=2 == 2 → 跳过
     s f
  [0,1,2,2,3,0,4,2]

i=3: nums[3]=2 == 2 → 跳过
     s   f
  [0,1,2,2,3,0,4,2]

i=4: nums[4]=3 != 2
     s     f
  [0,1,3,2,3,0,4,2] ← 复制 3 到 slow 位置

i=5: nums[5]=0 != 2
       s     f
  [0,1,3,0,3,0,4,2] ← 复制 0 到 slow 位置

i=6: nums[6]=4 != 2
         s     f
  [0,1,3,0,4,0,4,2] ← 复制 4 到 slow 位置

i=7: nums[7]=2 == 2 → 跳过
         s       f
  [0,1,3,0,4,0,4,2]

退出循环，返回 slow = 5
前 5 个元素: [0,1,3,0,4]
```

### ⚠️ 边界情况

```java
// 边界 1: 所有元素都要移除
nums = [3,3,3], val = 3
输出: 0, nums = [_,_,_]
✅ slow 一直为 0，返回 0

// 边界 2: 没有元素要移除
nums = [1,2,3], val = 4
输出: 3, nums = [1,2,3]
✅ fast 和 slow 同步移动

// 边界 3: 空数组
nums = [], val = 1
输出: 0
✅ 循环不执行，返回 0
```

### 📊 复杂度分析

- **时间复杂度**: O(n)
  - 只遍历数组一次
- **空间复杂度**: O(1)
  - 只用了 2 个指针变量

### 🔑 核心模式总结

**模式名称**: 快慢指针

**适用场景**:
- 移除元素
- 删除重复项
- 原地修改数组

**关键技巧**:
1. 慢指针标记有效位置
2. 快指针探索整个数组
3. 快指针找到有效元素时，复制到慢指针位置
4. 慢指针只在有效时才前移

---

## 🎯 两种双指针对比

| 特性 | 从后往前的双指针 | 快慢指针 |
|------|------------------|----------|
| **方向** | 从后往前 | 从前往后 |
| **指针数量** | 3 个（p1, p2, p） | 2 个（fast, slow） |
| **移动规则** | 选择较大的，指针前移 | 快指针快，慢指针慢 |
| **适用场景** | 合并有序数组 | 移除、去重 |
| **核心优势** | 避免覆盖 | 原地修改 |
| **时间复杂度** | O(m + n) | O(n) |
| **空间复杂度** | O(1) | O(1) |

---

## ✅ 今日练习

### 练习 1：手写 LeetCode 88
```java
// 不看参考，15 分钟内写出
public void merge(int[] nums1, int m, int[] nums2, int n) {
    // 你的代码
    
}
```

### 练习 2：手写 LeetCode 27
```java
// 不看参考，10 分钟内写出
public int removeElement(int[] nums, int val) {
    // 你的代码
    
}
```

### 练习 3：思考题
1. 如果 LeetCode 88 从前往后合并，如何避免覆盖？
   - 提示：需要额外空间

2. 如果 LeetCode 27 要保持元素的相对顺序怎么办？
   - 提示：当前方法已经保持顺序

3. 快慢指针还能解决什么问题？
   - 提示：删除有序数组中的重复项（LeetCode 26）

---

## 📝 学习检查清单

### LeetCode 88 - 合并两个有序数组
- [ ] 理解为什么从后往前
- [ ] 能画出三个指针的位置
- [ ] 能独立写出代码
- [ ] 理解边界情况处理

### LeetCode 27 - 移除元素
- [ ] 理解快慢指针的概念
- [ ] 能画出快慢指针的移动
- [ ] 能独立写出代码
- [ ] 理解为什么空间复杂度是 O(1)

### 双指针模式
- [ ] 理解双指针避免嵌套循环
- [ ] 能区分两种双指针的应用场景
- [ ] 能解释时间复杂度从 O(n²) 优化到 O(n)

---

## 🚀 下一步

完成今天的学习后：
1. ✅ 更新复习计划（明天要复习今天的题目）
2. ✅ 记录掌握程度到学习进度
3. ✅ 准备明天的题目（LeetCode 26, 80）

---

**创建日期**: 2026-02-05
**预计完成时间**: 60 分钟
**复习时间**: 2026-02-06（明天）
